services:
  manual-trading-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: manual-trading-agent
    restart: unless-stopped
    env_file:
      - .env
    # Environment variables expected in .env:
    #   OANDA_API_KEY=...
    #   OANDA_ACCOUNT_ID=...
    #   OANDA_ENVIRONMENT=practice  # or live
    #   TELEGRAM_BOT_TOKEN=...
    #   TELEGRAM_CHAT_ID=...
    environment:
      - TZ=America/Bogota
      - TELEGRAM_POLL_ENABLED=${TELEGRAM_POLL_ENABLED:-true}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config:ro
      - ./results:/app/results
    ports:
      - "127.0.0.1:9090:9090"  # Prometheus metrics (optional, for future use)
    # Run scan every 15 minutes + poll Telegram commands continuously.
    # Local dev with prod token: use docker-compose.local.yml (see that file).
    entrypoint:
      [
        "/bin/sh",
        "-c",
        "if [ \"$${TELEGRAM_POLL_ENABLED:-true}\" = \"true\" ]; then python -u -m src.cli telegram-poll >> /app/logs/telegram.log 2>&1 & fi; while true; do echo '===' $$(date) '===' >> /app/logs/scan.log; python -u -m src.cli scan >> /app/logs/scan.log 2>&1; sleep 900; done",
      ]
    healthcheck:
      test: ["CMD", "python", "-m", "src.cli", "healthcheck"]
      interval: 5m
      timeout: 10s
      retries: 3
      start_period: 1m
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"

  # ---------------------------------------------------------------------------
  # OPTIONAL: Prometheus — uncomment to enable metrics scraping
  # Scrapes the agent's /metrics endpoint on port 9090.
  # ---------------------------------------------------------------------------
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: manual-trading-prometheus
  #   restart: unless-stopped
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus_data:/prometheus
  #   ports:
  #     - "9091:9090"
  #   command:
  #     - "--config.file=/etc/prometheus/prometheus.yml"
  #     - "--storage.tsdb.retention.time=30d"
  #   logging:
  #     driver: json-file
  #     options:
  #       max-size: "20m"
  #       max-file: "5"

  # ---------------------------------------------------------------------------
  # OPTIONAL: Grafana — uncomment to enable dashboards
  # Requires Prometheus to be enabled above.
  # Default login: admin / admin (change via GF_SECURITY_ADMIN_PASSWORD in .env)
  # ---------------------------------------------------------------------------
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: manual-trading-grafana
  #   restart: unless-stopped
  #   depends_on:
  #     - prometheus
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
  #     - GF_USERS_ALLOW_SIGN_UP=false
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #     - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
  #     - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
  #   ports:
  #     - "3000:3000"
  #   logging:
  #     driver: json-file
  #     options:
  #       max-size: "20m"
  #       max-file: "5"

# ---------------------------------------------------------------------------
# Named volumes — uncomment if Prometheus/Grafana are enabled above
# ---------------------------------------------------------------------------
# volumes:
#   prometheus_data:
#   grafana_data:
